#!/bin/sh

. /lib/functions/system.sh

case "$(board_name)" in
	cambiumnetworks,xv2-2|\
	cambiumnetworks,xv2-2t0|\
	cambiumnetworks,xv2-2t1|\
	cambiumnetworks,xe3-4|\
	cambiumnetworks,xe3-4tn) ;;
	*) exit 0 ;;
esac

# The RAM image joins the management LAN as a DHCP client. Never offer DHCP
# or router advertisements to the site while testing an AP.
uci -q set dhcp.lan.ignore='1'
uci -q set dhcp.lan.dhcpv4='disabled'
uci -q set dhcp.lan.dhcpv6='disabled'
uci -q set dhcp.lan.ra='disabled'
uci -q commit dhcp

uci -q set system.@system[0].hostname='jaguar-recovery'
uci -q commit system

exit 0
